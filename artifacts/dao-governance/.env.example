# Frontend (Vite) environment variables
# Copy to artifacts/dao-governance/.env for local development
# (or export them in your shell before running)

PORT=5173
BASE_PATH=/

# Optional: override which port the API server is running on
# (defaults to 8080 when not set)
# API_PORT=8080
